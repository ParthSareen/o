# o

Project context lives in @AGENTS.md — read that first.

This is the **o** agent harness workspace (`github.com/ParthSareen/o`), not
the ollama repo. Build with `go install ./cmd/o`, test with `go test ./...`,
run the macOS app with `sh app/scripts/build-app.sh`. See @AGENTS.md for the
full layout, flags, and conventions.